apiVersion: v1
kind: Service
metadata:
  name: {{ include "hyperledger-fabric-org.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "hyperledger-fabric-org.name" . }}
    helm.sh/chart: {{ include "hyperledger-fabric-org.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "hyperledger-fabric-org.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
