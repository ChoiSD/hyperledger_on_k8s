apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hyperledger-fabric-org.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "hyperledger-fabric-org.name" . }}
    helm.sh/chart: {{ include "hyperledger-fabric-org.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "hyperledger-fabric-org.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
